<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h4> Player Details </h4>
    <ul>
      <img src='<%= playerPic %>'/>
      <li> Name: <%= playerDetails[0]%></li>
      <li> Team: <%= playerDetails[1]%></li>
      <li> Position: <%= playerDetails[2]%></li>
      <li> Games: <%= playerDetails[3]%></li>
      <li> PPG: <%= Math.round((playerDetails[4]/82)*10)/10%></li>
      <li> FG%: <%= playerDetails[5]%></li>
      <li> FT%: <%= playerDetails[6]%></li>
      <li> RPG: <%= playerDetails[7]%></li>
      <li> APG: <%= Math.round((playerDetails[8]/82)*10)/10%></li>
      <li> BPG: <%= Math.round((playerDetails[9]/82)*10)/10%></li>
      <li> SPG: <%= Math.round((playerDetails[10]/82)*10)/10%></li>
      <li> 3PT/game: <%= Math.round((playerDetails[11]/82)*10)/10%></li>
      <li> MPG: <%= Math.round((playerDetails[12]/82)*10)/10%></li>
      <li> TOPG: <%= Math.round((playerDetails[13]/82)*10)/10%></li>
    </ul>
    <form action="/new" method="GET" autocomplete="off">
      <button type="submit">Back to Search</button>
    </form>
  </body>
</html>
